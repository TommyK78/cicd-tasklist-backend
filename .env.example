# Exemple de configuration - à copier en .env (le .env réel n'est jamais versionné)
# Chaîne de connexion à la base MySQL (aucun secret réel ne doit être committé)
DATABASE_URL=mysql://user:password@localhost:3306/tasklist

# Port d'écoute de l'API
PORT=3001
